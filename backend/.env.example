# Copy to .env and fill in values before running the backend.

# PostgreSQL connection string
DATABASE_URL=postgresql://billsplitter:billsplitter@localhost:5433/billsplitter

# The base URL the frontend is served from (used for CORS)
APP_BASE_URL=http://localhost:5173

# Port the C++ backend listens on (default: 8080)
PORT=8080
